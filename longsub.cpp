#include <iostream>
#include <cstring>
#define LIMIT 101
using namespace std;

int main() {
    int testcase;
    cin >> testcase;
    while(testcase--) {
        char num1[LIMIT];
        char num2[LIMIT];
        int num1len, num2len; 
        cin >> num1 >> num2;
        
        num1len = strlen(num1);
        num2len = strlen(num2);
        
        
    }
    
    return 0;
}