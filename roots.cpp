#include <iostream>
#include <cstdio>
#include <vector>

using namespace std;

double co[6];

vector<double> getAnswer(double co[], int degree);

int main() {
    int tcase;
    cin >> tcase;
    while(tcase--) {
        int n;
        cin >> n;
        for(int i=0;i<=n;i++) {
            cin >> co[i];
        }


    }
    return 0;
}

vector<double> getAnswer(double co[], int degree) {
    
}
